[package]
name = "universal-stickers-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["staticlib"]

[dependencies]
cxx = "1.0"
universal-stickers-core = { path = "../core" }

[build-dependencies]
cxx-build = "1.0"

